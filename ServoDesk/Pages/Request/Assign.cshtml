@page
@model Hweb.Pages.Request.AssignModel

@{
    ViewData["Title"] = "Assign";
}
<head></head>
<h1>Assign</h1>

<h4>Assignment success</h4>
<hr />
@*<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="userRequests.username" class="control-label"></label>
                <input asp-for="userRequests.username" class="form-control" />
                <span asp-validation-for="userRequests.username" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="userRequests.requestID" class="control-label"></label>
                <select asp-for="userRequests.requestID" class ="form-control" asp-items="ViewBag.requestID"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>*@

<div>
    <a asp-page="./RequestsList">Back to List</a>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
