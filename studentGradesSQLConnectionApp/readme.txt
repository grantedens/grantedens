This project uses the System.Data.SqlClient namespace to connect a visual studio interface to a database. 
The applications allows the user to view data on student grades and modify/update the data through the GUI
